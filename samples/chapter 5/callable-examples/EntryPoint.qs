namespace CallableExamples {
    
    @EntryPoint()
    /// # Summary
    /// The entry point for the callable example.
    /// Uncomment the lines calling the examples you want to run.
    operation RunAllExamples() : Unit {
        // BasicsExamples();
        // GatesMeasurementsExamples();
        // AdjointControlledExamples();
        // FunctionalElementsExamples();
        // TypeParameterizedExamples();
    }
}
