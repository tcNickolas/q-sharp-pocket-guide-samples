namespace LibraryExamples {
    
    @EntryPoint()
    /// # Summary
    /// The entry point for the libraries example.
    /// Uncomment the lines calling the examples you want to run.
    operation RunAllExamples() : Unit {
        // ConvertExamples();
        // LogicalExamples();
        // BitwiseExamples();
    }
}
