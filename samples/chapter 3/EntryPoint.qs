namespace ExpressionsExamples {
    
    @EntryPoint()
    /// # Summary
    /// The entry point for the expressions example.
    /// Uncomment the lines calling the examples you want to run.
    operation RunAllExamples() : Unit {
        // EqualityExamples();
        // ComparisonExamples();
    }
}
